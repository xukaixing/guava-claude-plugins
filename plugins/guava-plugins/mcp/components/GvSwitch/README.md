# GvSwitch MCP

随 **guava-plugins** 分发。运行时不依赖 press / guava-ui 源码。

| 数据 | 来源 |
| --- | --- |
| 用法 / API / 示例 | 本目录 `usage.json` |
| props / emits | 消费项目 `node_modules/guava-ui`（`CLAUDE_PROJECT_DIR`） |
| 实现源 | `guava-ui packages/GvSwitch/src/GvSwitch.vue` |

注册名：`gv-switch`

> **用法**：GvSwitch 通常在 `GvTable` 列的 `render` 函数中使用，用于渲染开关控件。
