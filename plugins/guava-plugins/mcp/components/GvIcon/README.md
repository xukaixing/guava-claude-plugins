# GvIcon MCP

随 **guava-plugins** 分发。运行时不依赖 press / guava-ui 源码。

| 数据 | 来源 |
| --- | --- |
| 用法 / API / 示例 | 本目录 `usage.json` |
| props / emits | 消费项目 `node_modules/guava-ui`（`CLAUDE_PROJECT_DIR`） |
| 实现源 | `guava-ui packages/GvIcon/src/GvIcon.vue` |
| 图标列表 | `src/views/iconsMng/font-icons.ts` + `el-icons.ts` + `svg-icons.ts` |

注册名：`gv-icon`

> **图标来源**：使用 GvIcon 时，优先从 `src/views/iconsMng/` 目录下的图标列表中选择。
