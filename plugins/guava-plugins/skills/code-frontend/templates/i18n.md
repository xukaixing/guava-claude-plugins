# 多语言

> [\_shared.md](../_shared.md) · [config-parser.md](../config-parser.md#i18n)

按 YAML `i18n` 决定。**默认 `false` = 仅中文**。

---

## 1. i18n 开关

| `i18n` | 行为 |
| ------ | ---- |
| `false`（**默认**） | **不更新** `zh-CN.ts` / `en.ts`；模板与 label 硬编码中文，不走 `t()` |
| `true` | 更新 `zh-CN.ts` + `en.ts` 的 `<i18nKey>` 分组；模板 / label 使用 `t('i18nKey.xxx')` |

---

## 2. 规则（i18n: true 时生效）

| 项 | 规则 |
| ---- | ---- |
| 分组 key | camelCase 特性名（如 `userMng`） |
| 中文值 | 自然中文 |
| 英文值 | 仅首字母大写（如 "User name"） |
| Keys | camelCase |

### 新建 vs 更新

| 场景 | 行为 |
| ---- | ---- |
| `<i18nKey>` 分组不存在 | 在 `};` 前**追加**新分组 |
| `<i18nKey>` 分组已存在 | **替换**整个分组 |

---

## 3. 模板

### zh-CN.ts

```typescript
<i18nKey>: {
  <fieldKey>: '<中文标签>',
  add<Feature>: '新增<Feature>',
  edit<Feature>: '编辑<Feature>',
  delete<Feature>: '删除<Feature>',
  <feature>Management: '<Feature>管理',
  deleteSuccess: '删除成功',
  deleteConfirm: '确认删除该记录？',
},
```

### en.ts

```typescript
<i18nKey>: {
  <fieldKey>: '<English label>',
  add<Feature>: 'Add <feature>',
  edit<Feature>: 'Edit <feature>',
  delete<Feature>: 'Delete <feature>',
  <feature>Management: '<Feature> management',
  deleteSuccess: 'Delete successfully',
  deleteConfirm: 'Confirm to delete this record?',
},
```

---

## 4. form-only

**不生成** add / edit / delete / deleteConfirm 等 CRUD key。

```typescript
<i18nKey>: {
  <fieldKey>: '<中文标签>',
  <feature>Title: '<配置 title>',
},
```

---

## 5. tabs

在 crud-module i18n 基础上，为每个 `tabs[]` 追加 Tab label key：

| tabs[].name | i18n key |
| ----------- | -------- |
| `list` | `tabList` |
| `edit` | `tabEdit` |
